## Building inline emails

To build project install npm, node and gulp. Then run:

```
gulp
```

All emails will be built into the dist folder. 

Important: images have to be uploaded to an external storage service.